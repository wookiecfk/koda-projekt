#include <iostream>
#include <cctype>
#include <stdlib.h>

enum chosenOption {
	HUFFMAN_STATIC_CODER = 1,
	HUFFMAN_STATIC_DECODER = 2,
	HUFFMAN_DYNAMIC_CODER = 3,
	HUFFMAN_DYNAMIC_DECODER = 4,
	FILE_GENERATOR = 5,
	EXIT = 0
};
